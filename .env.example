# Enterprise A11y Quality Gate — Environment Variables
# Copy to .env and fill in values as needed.

# Post-navigation settle time (ms). Increase for SPAs that render slowly.
A11Y_SETTLE_MS=8000

# Max time (ms) to wait for meaningful DOM content before scanning.
A11Y_CONTENT_WAIT_MS=30000

# WebAIM WAVE API key (only required for `npm run a11y:wave`).
# All other engines run 100% locally with no tokens.
# WAVE_API_KEY=your_wave_api_key_here

# Target URL for Playwright CI test spec (`npm run test:a11y`).
TARGET_URL=https://example.com
